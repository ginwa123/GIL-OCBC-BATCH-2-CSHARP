import { CustomfilesizePipe } from './customfilesize.pipe';

describe('CustomfilesizePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomfilesizePipe();
    expect(pipe).toBeTruthy();
  });
});
